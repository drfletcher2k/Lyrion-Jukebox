services:
  jukebox:
    build: .
    ports:
      - "0.0.0.0:3200:3200"
    env_file:
      - ./server/.env
    volumes:
      - jukebox-data:/app/server/data
    restart: unless-stopped

volumes:
  jukebox-data:
